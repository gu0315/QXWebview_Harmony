{
  "module": {
    "name": "entry",
    "type": "entry",
    "description": "$string:module_desc",
    "mainElement": "EntryAbility",
    "deviceTypes": [
      "phone",
      "tablet"
    ],
    // openMap 检测第三方地图是否安装(bundleManager.canOpenLink)所需。
    // 集成方需在自己的入口模块声明相同 querySchemes。
    "querySchemes": [
      // 华为花瓣:用 geo: 唤起(它自己的 maps://navigation 进不了导航),
      // 但 geo: 是 opaque URI 匹配不上 skill,所以「是否已安装」靠 petalmaps 探测。
      "geo",
      "petalmaps",
      // 鸿蒙版高德(com.amap.hmapp)注册的是 amapuri,不是 Android 版的 androidamap。
      // 真机 `bm dump -n com.amap.hmapp` 可核对。androidamap 一并保留,兼容装了
      // Android 兼容版高德的设备。
      "amapuri",
      "androidamap",
      "baidumap",
      "qqmap",
      // H5 跳转 weixin://(唤起微信/小程序)。startAbility 唤起本身不需要声明,
      // 声明后才能用 canOpenLink 判断微信是否已安装。
      "weixin"
    ],
    "deliveryWithInstall": true,
    "installationFree": false,
    "pages": "$profile:main_pages",
    "abilities": [
      {
        "name": "EntryAbility",
        "srcEntry": "./ets/entryability/EntryAbility.ets",
        "description": "$string:EntryAbility_desc",
        "icon": "$media:startIcon",
        "label": "$string:EntryAbility_label",
        "startWindowIcon": "$media:startIcon",
        "startWindowBackground": "$color:start_window_background",
        "exported": true,
        "skills": [
          {
            "entities": ["entity.system.home"],
            "actions": ["action.system.home"]
          }
        ]
      }
    ],
    "requestPermissions": [
      { "name": "ohos.permission.INTERNET" },
      {
        "name": "ohos.permission.APPROXIMATELY_LOCATION",
        "reason": "$string:reason_location",
        "usedScene": { "when": "inuse" }
      },
      {
        "name": "ohos.permission.LOCATION",
        "reason": "$string:reason_location",
        "usedScene": { "when": "inuse" }
      },
      {
        "name": "ohos.permission.ACCESS_BLUETOOTH",
        "reason": "$string:reason_bluetooth",
        "usedScene": { "when": "inuse" }
      },
      {
        "name": "ohos.permission.CAMERA",
        "reason": "$string:reason_camera",
        "usedScene": { "when": "inuse" }
      }
    ]
  }
}
